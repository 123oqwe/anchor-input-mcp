{
  "name": "@anchor/input-mcp",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "Cross-platform desktop GUI control as MCP server (keystroke / click / type / screenshot). Mac (osascript + screencapture), Windows (PowerShell), Linux (xdotool).",
  "bin": { "anchor-input-mcp": "dist/index.js" },
  "main": "dist/index.js",
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx --watch src/index.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3"
  }
}
